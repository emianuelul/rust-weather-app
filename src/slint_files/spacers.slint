export component VerticalSpacer inherits Rectangle {
    in property <float> stretch: 0.5;
    vertical-stretch: stretch;

    background: transparent;
}

export component HorizontalSpacer inherits Rectangle {
    in property <float> stretch: 0.5;
    horizontal-stretch: stretch;

    background: transparent;
}
